import { FC } from 'react';
import PageTemplate from '../Common/PageTemplate';

const Contacts: FC = () => {
  return <PageTemplate title="Редагувати контакти">Contacts</PageTemplate>;
};

export default Contacts;
