import { FC } from 'react';
import PageTemplate from '../Common/PageTemplate';

const AddEvent: FC = () => {
  return <PageTemplate title="Додати подію">Add event</PageTemplate>;
};

export default AddEvent;
